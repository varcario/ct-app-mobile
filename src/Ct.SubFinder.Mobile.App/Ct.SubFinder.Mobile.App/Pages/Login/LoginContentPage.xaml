<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Ct.SubFinder.Mobile.App.Pages.Login.LoginContentPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <Grid BackgroundColor="{StaticResource PrimaryDarkColor}">
            <Grid.RowDefinitions>
                <RowDefinition Height="35*"/>
                <RowDefinition Height="100*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="60*"/>
                <ColumnDefinition Width="20*"/>
            </Grid.ColumnDefinitions>
            <StackLayout Grid.Row="1" Grid.ColumnSpan="3">
                <Entry Text="{Binding EmailAddress}" Placeholder="Email" />
                <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="True" />
                <Button Text="Next" Command="{Binding LogInCommand}"  Margin="0,35,0,0"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>