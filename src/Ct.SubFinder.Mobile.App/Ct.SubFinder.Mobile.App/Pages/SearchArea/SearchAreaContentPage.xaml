<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             x:Class="Ct.SubFinder.Mobile.App.Pages.SearchArea.SearchAreaContentPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding ZipCode}" Placeholder="Zip Code"/>
            <Label Text="Search Radius"/>
            <Label Text="{Binding Miles}"/>
            <Slider Maximum="200" Minimum="5" Value="{Binding Miles}">
                <Slider.Behaviors>
                    <b:EventToCommandBehavior EventName="ValueChanged"
                      Command="{Binding MilesChangedCommand}"
                      CommandParameter="{Binding Value}" />
                </Slider.Behaviors>
            </Slider>
            <Button Text="Done" Command="{Binding DoneCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>